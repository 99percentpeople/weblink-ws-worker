# Local development: copy these values to .dev.vars.
# Production: configure them as Secrets in Cloudflare Worker Settings.
TURN_KEY_ID=replace-with-turn-key-id
TURN_KEY_API_TOKEN=replace-with-turn-key-api-token
